절두체를 만들자.

왼쪽 상단 앞쪽 : 0

절두체 컴포넌트를 갖고있는 객체가 자기 자신을 컬링해주라고 하자.
--> pTransForm을 받아와서 충돌 처리.

카메라 -> (프레임마다)월드Matrix 역행렬 Set -> 객체 -> Get -> 변환
	  -> View, Proj Update호출
Update_Frustum
{
	DXCoord에서 z연산을 따로 할 필요가 없다. (함수에서 해준다.)
	View Proj 역행렬을 각 곱해 월드행렬로 보내준다.
}


평면의 수직인 벡터를 구한다. --> 이 면 상에 존재하고있는 1개의 점이 필요(높이 개념)
평면방적식을 구성하기 위한 점 3개를 넣어 주면 된다.
법선 벡터를 이루고 있는 평면이 평면의 '앞면'이다 라고 이야기 한다.

D3DXPlaneFromPoints(); // 3개의 점을 주면 평면의 방정식으로 만들어준다.
D3DXPlaneDot() // xyz 대입 ax+by+cz+d = 0; --> 값 구하기 (0 > 값, 0 == 값, 0 < 값)
-->하나라도 양이라면 평면 밖
-->모두 다 음이면 평면 안
--> 하나라도 0이면 평면 위

